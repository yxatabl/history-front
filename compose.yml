version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:5000"  # Maps host port 80 to container port 5000
    volumes:
      - .:/app     # Mounts current directory into /app in container
    environment:
      - FLASK_ENV=development
    restart: unless-stopped